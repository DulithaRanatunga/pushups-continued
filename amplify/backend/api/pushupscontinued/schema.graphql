type Note @model @auth(rules: [ { allow: public } ] ){
  id: ID!
  name: String!
  description: String
}

type Bank @model @auth(rules: [ { allow: public } ] ){
  id: ID!
  date: String!
  count: Int!
}